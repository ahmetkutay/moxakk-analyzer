package com.moxakk.analyzer.config;

import com.moxakk.analyzer.scraping.football.util.WebDriverFactory;
import org.springframework.context.annotation.Configuration;

/**
 * Configuration class for Selenium WebDriver.
 * This class is now just a placeholder as we're using WebDriverFactory instead.
 */
@Configuration
public class WebDriverConfig {
    // WebDriver configuration is now handled by WebDriverFactory
}